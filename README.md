# simpleFlaskApp

General setup:
```
# Create and activate virtualenv
$ mkvirtualenv env1
$ workon env1

# Install Flask in project dir
$ pip install Flask

# Dependency manifest
$ pip freeze >> requirements.txt
```
